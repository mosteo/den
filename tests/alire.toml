name = "den_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
den = "*"

[[pins]]
den = { path = ".." }
c_strings = { path = "../../c_strings" }

[build-profiles]
den       = "validation"
den_tests = "validation"
