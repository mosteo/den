name = "den_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
den = "*"

[[pins]]
den = { path = ".." }

[build-profiles]
den       = "validation"
den_tests = "validation"
