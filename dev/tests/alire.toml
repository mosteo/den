name = "dev_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
dev = "*"

[[pins]]
dev = { path = ".." }

[build-profiles]
dev       = "validation"
dev_tests = "validation"
